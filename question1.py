def solution(S):
    n = len(S)
    m = len(S[0])


# picks first word in list
    for i in range(n):
        #picks second word in list ensuring a word is not repeated
        for j in range(i+1, n):
            similar_position = {k for k in range(m)}


    return []